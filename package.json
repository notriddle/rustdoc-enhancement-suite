{
  "repository": {},
  "license": "APACHE-2.0 OR MIT",
  "scripts": {
    "lint": "web-ext lint --source-dir ./extension",
    "start": "web-ext run --source-dir ./extension",
    "start:firefox": "web-ext run --target firefox-desktop --source-dir ./extension",
    "start:chrome": "web-ext run --target chromium --source-dir ./extension",
    "build": "web-ext build --overwrite-dest --source-dir ./extension"
  },
  "devDependencies": {
    "web-ext": "^6.6.0"
  }
}
